<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_main">

    <!-- Status Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:background="@color/bg_header">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/app_name"
            android:textColor="@color/signal_green"
            android:textSize="14sp"
            android:textStyle="bold"
            android:fontFamily="@font/jetbrains_mono" />

        <TextView
            android:id="@+id/statusBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="connecting..."
            android:textColor="@color/text_dim"
            android:textSize="10sp"
            android:fontFamily="@font/jetbrains_mono" />
    </LinearLayout>

    <!-- Content Area (chat + files swap here) -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- ═══ CHAT TAB ═══ -->
        <LinearLayout
            android:id="@+id/chatContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <ScrollView
                android:id="@+id/scrollView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:fillViewport="true"
                android:background="@color/terminal_bg">

                <TextView
                    android:id="@+id/terminalOutput"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="16dp"
                    android:textColor="@color/text_primary"
                    android:textSize="13sp"
                    android:fontFamily="@font/jetbrains_mono"
                    android:lineSpacingExtra="4dp"
                    android:textIsSelectable="true" />
            </ScrollView>

            <!-- Thinking Indicator -->
            <LinearLayout
                android:id="@+id/thinkingIndicator"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:background="@color/bg_surface"
                android:visibility="gone">

                <View
                    android:layout_width="6dp"
                    android:layout_height="6dp"
                    android:background="@color/signal_green" />

                <TextView
                    android:id="@+id/thinkingText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/thinking"
                    android:textColor="@color/signal_green"
                    android:textSize="11sp"
                    android:fontFamily="@font/jetbrains_mono"
                    android:layout_marginStart="8dp" />
            </LinearLayout>

            <!-- Pending Attachments Indicator -->
            <TextView
                android:id="@+id/attachIndicator"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp"
                android:paddingVertical="6dp"
                android:background="@color/signal_green_bg"
                android:textColor="@color/signal_green"
                android:textSize="11sp"
                android:fontFamily="@font/jetbrains_mono"
                android:visibility="gone" />

            <!-- Input Bar -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:padding="8dp"
                android:background="@color/bg_surface">

                <ImageButton
                    android:id="@+id/attachButton"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_attach"
                    android:scaleType="centerInside"
                    android:contentDescription="Attach image"
                    android:padding="6dp" />

                <EditText
                    android:id="@+id/commandInput"
                    android:layout_width="0dp"
                    android:layout_height="44dp"
                    android:layout_weight="1"
                    android:layout_marginStart="4dp"
                    android:hint="@string/command_hint"
                    android:textColorHint="@color/text_dim"
                    android:textColor="@color/text_primary"
                    android:textSize="13sp"
                    android:fontFamily="@font/jetbrains_mono"
                    android:background="@drawable/bg_input_field"
                    android:paddingHorizontal="14dp"
                    android:singleLine="true"
                    android:imeOptions="actionSend"
                    android:enabled="false" />

                <ImageButton
                    android:id="@+id/sendButton"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_send_button"
                    android:src="@drawable/ic_send"
                    android:scaleType="centerInside"
                    android:contentDescription="@string/send"
                    android:padding="10dp"
                    android:enabled="false" />
            </LinearLayout>
        </LinearLayout>

        <!-- ═══ FILES TAB ═══ -->
        <LinearLayout
            android:id="@+id/filesContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:visibility="gone">

            <!-- Breadcrumb / path bar -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="16dp"
                android:paddingVertical="10dp"
                android:background="@color/bg_surface">

                <ImageButton
                    android:id="@+id/filesBackButton"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:src="@drawable/ic_back"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Back"
                    android:padding="4dp"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/filesPathBar"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="8dp"
                    android:text="@string/files_title"
                    android:textColor="@color/signal_green"
                    android:textSize="12sp"
                    android:fontFamily="@font/jetbrains_mono"
                    android:singleLine="true"
                    android:ellipsize="start" />
            </LinearLayout>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/drawer_divider" />

            <!-- File list -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/filesRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="@color/bg_main" />

            <!-- Empty state -->
            <TextView
                android:id="@+id/filesEmptyText"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:gravity="center"
                android:text="@string/files_empty"
                android:textColor="@color/text_dim"
                android:textSize="13sp"
                android:fontFamily="@font/jetbrains_mono"
                android:visibility="gone" />
        </LinearLayout>

        <!-- ═══ SESSIONS TAB ═══ -->
        <LinearLayout
            android:id="@+id/sessionsContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:visibility="gone">

            <!-- Sessions header -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="16dp"
                android:paddingVertical="10dp"
                android:background="@color/bg_surface">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/sessions_title"
                    android:textColor="@color/signal_green"
                    android:textSize="12sp"
                    android:fontFamily="@font/jetbrains_mono" />

                <ImageButton
                    android:id="@+id/newSessionButton"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:src="@drawable/ic_add"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/new_session"
                    android:padding="4dp" />
            </LinearLayout>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/drawer_divider" />

            <!-- Session list -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/sessionsRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="@color/bg_main" />

            <!-- Empty state -->
            <TextView
                android:id="@+id/sessionsEmptyText"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:gravity="center"
                android:text="@string/sessions_empty"
                android:textColor="@color/text_dim"
                android:textSize="13sp"
                android:fontFamily="@font/jetbrains_mono"
                android:visibility="gone" />
        </LinearLayout>
    </FrameLayout>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bg_surface"
        app:itemIconTint="@color/tab_active"
        app:itemTextColor="@color/tab_active"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
